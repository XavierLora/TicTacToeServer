/**
 * The `model` package contains classes related to modeling events and users.
 *
 * This package includes the following classes:
 * - `Event`: Represents an event with various attributes such as event ID, sender, opponent, status, and more.
 * - `User`: Represents a user with attributes like username, password, display name, and online status.
 *
 * These classes are designed to provide a foundational structure for managing events and user information
 * within your application. They offer methods and properties for creating, modifying, and querying event and
 * user data.
 *
 * @since 1.0
 */
package model;
